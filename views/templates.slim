

template id="bussTemplate"
    article.buss.elemBox
        .bussName.bussElem.elem
        .bussDepTime.bussElem.elem


template id="weatherTemplate"
    article.weather.elemBox
        .time.weatherElem.elem
        .temperature.weatherElem.elem
        .weatherSymbol.weatherElem.elem
            img
        .windSpeed.weatherElem.elem
        / img.windDirection.weatherElem.elem
        .gust.weatherElem.elem
